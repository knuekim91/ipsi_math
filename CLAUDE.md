# ipsi_math — 작업 규약

이 파일은 Claude가 이 저장소에서 일할 때 따르는 규칙이다. 세션이 바뀌어도 이 문서만 읽으면
동일한 방식으로 이어서 작업할 수 있어야 한다.

## 0. 전제

- 학습자: 나은 (고3, 수Ⅰ·수Ⅱ + 확률과 통계 선택)
- 목표 시험: **2027학년도 수능 — 2026년 11월 19일(목)**
- 진행 방식: 학습자가 **모르는 문제를 가져오면**, 그것을 분류하고 핵심 요소를 뽑아
  **같은 수준의 유사문항을 개발**하여 **인쇄용 PDF**로 돌려준다.
- D-day는 매번 계산해서 말한다: `python -c "import datetime as d;print((d.date(2026,11,19)-d.date.today()).days)"`

## 1. 단원 체계 (고정 — 임의로 바꾸지 않는다)

| 폴더 | ID 접두 | 범위 |
|---|---|---|
| `units/01-지수로그` | `EXP` | 지수·로그의 계산, 지수·로그함수, 방정식·부등식 |
| `units/02-삼각함수` | `TRI` | 삼각함수의 그래프, 사인·코사인법칙, 도형 활용 |
| `units/03-수열` | `SEQ` | 등차·등비, 시그마, 귀납적 정의, 수열의 규칙 |
| `units/04-극한연속` | `LIM` | 함수의 극한, 연속, 미분계수의 정의 |
| `units/05-미분` | `DIF` | 도함수, 접선, 극값, 그래프 개형, 속도·가속도, 미분가능성 |
| `units/06-적분` | `INT` | 부정·정적분, 적분방정식, 넓이, 절댓값 적분 |
| `units/07-경우의수확률` | `PRB` | 순열·조합·중복조합, 이항정리, 확률, 조건부확률 |
| `units/08-통계` | `STA` | 확률분포, 이항분포, 정규분포, 표본평균 |

새 문항 ID는 해당 접두사의 **다음 번호**를 쓴다 (`EXP-005`, `PRB-007` …).
직접 만든 유사문항은 뒤에 `-V1`, `-V2`를 붙인다 (`EXP-004-V1`).

## 2. 문항 파일 규격

경로: `units/<단원>/problems/<ID>.md`

**하위 폴더를 써도 된다.** 빌드가 `problems/` 아래를 재귀로 훑는다.
교재처럼 한 묶음으로 들어오는 것은 폴더로 묶어 둔다.

```
units/05-미분/problems/DIF-J21.md              ← 모평은 그대로
units/05-미분/problems/2027수능완성/DIF-C01.md  ← 교재는 폴더에
```

폴더 이름은 **화면에 영향을 주지 않는다.** 화면 분류는 오직 `exam` 필드가 정한다.
파일 정리용일 뿐이다. `_` 로 시작하는 파일과 폴더는 건너뛴다.

```markdown
---
id: EXP-005
unit: 01-지수로그
topic: 로그의 성질          # 한 줄짜리 소분류
level: 4점                  # 2점 / 3점 / 4점
difficulty: 중상            # 하 / 중 / 중상 / 상
source: 2027-09모평 10번    # 또는 "자체 개발 (EXP-004 변형)"
exam: 2027-09모평           # 기출일 때만. 사이트의 시험 선택 버튼 근거
origin: 기출                # 기출 | 유사문항 | 오답노트  ← 웹앱의 색상 배지 근거
core: "한 문장으로 쓴 핵심 발상"
tags: [태그1,태그2]
status: seed                # 아래 4절 참조
added: 2026-09-04
answer: ③
---

## 문제
## 발상
## 풀이
## 다른 풀이  ← 선택. 접근 자체가 다른 두 번째 풀이가 있을 때만 쓴다
## 함정      ← 선택
## 노하우
```

`## 다른 풀이`는 **계산 순서만 바꾼 것이 아니라 발상이 다른 경우**에만 넣는다.
(예: 13번을 중간값 정리 대신 F(p)=6+p로 직접 구성, 19번을 미분 대신 중근 조건으로)
넣을 때는 본 풀이와 마찬가지로 **파이썬으로 검증**하고, 어느 쪽이 시험장에서 빠른지 한 줄로 적는다.

- 본문은 **HTML 조각**으로 쓴다. 수식은 `<span class="m">`, 첨자는 `<sub>/<sup>`.
- 선택지는 `<div class="choices"><span>① …</span>…</div>`.
- 조건 상자(가/나)는 `<span class="cond m">…</span>`.
- 빌드 스크립트가 이 태그들을 그대로 PDF로 넘긴다. **마크다운 문법은 쓰지 않는다.**

### 수식 칸 안에 한글을 넣지 않는다

`to_mixed()` 는 한글 덩어리를 경계로 수식을 쪼갠 뒤 조각마다 `$...$` 를 씌운다.
그래서 **중괄호가 한글을 사이에 두고 갈라지면** `{` 가 열린 채 `$` 를 만나 KaTeX 가 깨진다.

```
<span class="m">B = {n | 1 ≤ n ≤ 12, n은 자연수}</span>     ← 깨진다
<span class="m">B = {n | 1 ≤ n ≤ 12}</span> (<span class="m">n</span>은 자연수)  ← 이렇게
```

`build/make_site.py` 의 `check_math()` 가 빌드할 때마다 전수 검사해 경고를 띄운다.
**경고가 뜨면 반드시 고치고 넘어간다.** 문장 끝의 `의 값은?`, `답 ④` 같은 한글은
괄호를 걸치지 않으므로 안전하고 경고도 뜨지 않는다.

### 그림이 있는 문항 — 원본은 오려 붙이고, 설명용은 직접 그린다

**그림을 말로 설명하지 않는다.** 두 갈래로 나뉜다.

| 어떤 그림인가 | 어떻게 | 왜 |
|---|---|---|
| **교재·기출 원본에 그림이 있다** | PDF 에서 오려 `docs/img/<ID>.png` | 다시 그리면 각도·길이가 미묘하게 달라져 문제와 어긋난다 |
| **내가 풀이를 위해 그린다** | SVG 를 직접 쓴다 | 원본이 없고, 테마 색을 따라가야 한다 |

#### 원본 그림 오려 오기

```bash
python build/figcrop.py --pdf "<교재 PDF>" --page 121 --list
python build/figcrop.py --pdf "<교재 PDF>" --page 121 --pick 0 --name TRI-D12
```

`--list` 가 그 쪽에서 찾은 그림을 보여 준다. **조건상자(가/나)는 그림이 아니므로 자동으로 빠진다**
(단 너비 248pt 를 꽉 채우는 둥근 사각형이 조건상자다).
점 이름(A, B, C …)은 도형이 아니라 글자라 도형 테두리에 안 잡히므로 따로 끌어들인다.
`--bbox x0,y0,x1,y1` 로 직접 지정할 수도 있다. 한 장에 20~50 KB 다.

**교재는 그림을 벡터로, 평가원 시험지는 래스터 이미지로 넣는다.** `--list` 가 둘 다 찾는다.
평가원 시험지는 한 그림이 두 장으로 쪼개져 들어가기도 해서 붙어 있는 것끼리 다시 합친다.
여러 쪽에 되풀이해 나오는 이미지(머리글 로고 등)와 윗머리 띠는 그림이 아니므로 뺀다.

원본 파일이 있는 곳:

```
바탕 화면/나은이/수능완성/2027 수능완성 확률과통계 .pdf
바탕 화면/나은이/수학모의고사문제/mathA_1_mun_A958ZU85.pdf   (2027 6월 모평)
바탕 화면/나은이/수학모의고사문제/mathA_1_mun_R9VI9L47.pdf   (2027 9월 모평)
```

명령이 문항에 붙일 조각을 그대로 찍어 준다.

```html
<div class="fig"><img src="img/TRI-D12.png" width="770" height="573"
  alt="그림 설명을 한 문장으로 적는다"></div>
```

- `width`/`height` 를 **반드시 넣는다.** 없으면 그림이 뜰 때 글이 밀려 내려간다.
- `alt` 는 화면낭독기와, 그림이 안 뜰 때를 위한 것이다. 한 문장으로 적는다.
- 화면에서는 **PNG 의 흰 바탕을 블렌드로 지운다.** 다크(기본)에서는 반전 뒤 `screen`,
  라이트에서는 그대로 두고 `multiply` 다. 그래서 그림이 카드 위에 흰 판으로 뜨지 않는다.
  선화(검은 선 + 흰 바탕)라서 되는 방법이므로, **색이 들어간 그림에는 쓰지 않는다.**
- **명암이 뜻을 가진 그림은 반전하면 안 된다.** 06모평 28번은 카드의 앞면(흰색)과
  뒷면(회색)을 명암으로 구분하는데, 반전하면 뒷면이 오히려 밝아져 거꾸로 읽힌다.
  이런 그림은 `<div class="fig plain">` 으로 감싼다. 흰 판에 그대로 얹는다.
  판단 기준은 **&lsquo;밝고 어두움이 서로 다른 물건을 뜻하는가&rsquo;** 다.
  칠한 영역이 &lsquo;여기가 구하는 부분&rsquo; 이라는 뜻일 뿐이면 반전해도 된다.
- 인쇄물에서는 반전도 블렌드도 끄고 원본 그대로 나간다.
- 기본은 **회색조**로 뽑는다. 수학 그림은 검은 선화라 용량이 절반이고 보기는 같다.
  색이 실제로 쓰인 그림에만 `--color` 를 준다.

#### 내가 그리는 그림 — SVG

**그림을 말로 설명하지 않는다.** 도형·그래프 문항은 문제 본문에 SVG 를 넣는다.
이미지 파일이나 외부 호스팅(구글 드라이브 등)은 **쓰지 않는다.**
드라이브 직접 링크는 막혀 있고, 나은이는 계정 없이 열기 때문이다.

```html
<div class="fig">
<svg viewBox="0 0 330 240" role="img" aria-label="그림 설명(화면낭독기용)">
  <path class="gd" d="…"/>   <!-- 안내선(점선) -->
  <path class="ax" d="…"/>   <!-- 축 -->
  <path class="cv" d="…"/>   <!-- 그래프 곡선 -->
  <path class="rg" d="…"/>   <!-- 칠한 영역 -->
  <circle class="pt" …/>     <!-- 채운 점 -->
  <circle class="po" …/>     <!-- 빈 점 -->
  <text x="…" y="…">−1</text>
  <text class="v" x="…" y="…">x</text>   <!-- 변수는 v (이탤릭) -->
</svg>
</div>
```

- **색을 직접 쓰지 않는다.** 클래스만 붙이면 단원 색과 테마(밝기)를 알아서 따라간다.
  `--figl` 이 라이트 42% / 다크 66% 로 갈리므로 흰 배경에서도 선이 보인다.
- 빈 점(`po`)은 배경색으로 채워 뚫려 보이게 한다. **극한 문항에서 이 구분이 답을 가른다.**
- `aria-label` 에 그림을 한 문장으로 적는다.
- `class="m"` 을 SVG 안에 쓰지 않는다. 수식 변환기가 건드린다.
- 도형 문항은 **그림을 먼저 그리고 풀이를 쓴다.** 풀이에서 &lsquo;그림에서 …&rsquo;로 가리킨다.
- **`build/print.css` 에도 같은 클래스의 규칙이 있다.** 인쇄물은 흑백이므로 색 대신
  검정으로 그린다. 한쪽만 고치면 화면과 인쇄물이 달라진다.

### exam — 어느 시험의 문항인가

기출 문항은 `exam`에 **`<학년도>-<시험>`** 형식으로 적는다. 시험은 세 가지만 쓴다.

| exam 값 | 사이트 표시 |
|---|---|
| `2027-06모평` | 2027학년도 > 6월 모평 |
| `2027-09모평` | 2027학년도 > 9월 모평 |
| `2021-수능` | 2021학년도 > 수능 |
| `2027-수능완성` | 2027 > 수능완성 |
| `2027-수능특강` | 2027 > 수능특강 |

시험이 아닌 **교재**도 같은 자리에 들어간다. `make_site.py` 의 `BOOKS` 에 이름과
정렬 순서가 들어 있고, 모의평가·수능 **뒤에** 놓인다. 새 교재를 쓰려면 거기 한 줄 추가한다.

사이트 상단은 **2단 구조**다. 1단은 **학년도 버튼**(문항이 있는 학년도만, 최신순),
학년도를 누르면 그 아래 2단으로 **그 학년도의 시험 버튼**(6월 → 9월 → 수능 순)이 펼쳐진다.
**번호가 시험마다 겹치므로 이 값이 없으면 목록이 섞인다.**

`build/make_site.py`의 `exam_parts()`가 이 문자열 하나로 학년도·이름·정렬순서를 만든다.
**새 시험을 추가할 때 코드는 건드리지 않는다.** 문항 파일에 `exam`만 제대로 넣으면
버튼·머리글·정렬이 전부 자동으로 생긴다.

문항 ID는 시험별로 접두 문자를 붙여 겹치지 않게 한다.

| 출처 | ID 예 | 규칙 |
|---|---|---|
| 2027-09모평 | `EXP-001` | 번호만 |
| 2027-06모평 | `EXP-J01` | `J` + 그 시험의 문항 번호 (J = June) |
| 나은이가 가져온 문제 | `DIF-W01` | `W` + 일련번호 (W = wrong). 학년도가 제각각이라 순번으로 준다 |
| 2027-수능완성 | `DIF-C10` | **회차 문자 + 문항 번호 두 자리.** 실전 모의고사 1회=`C`, 2회=`D`, 3회=`E`, 4회=`F`, 5회=`G` |

실전 모의고사처럼 **회차가 여러 개인 교재**는 회차마다 문자를 하나씩 올린다.
그러면 `DIF-D22` 가 &lsquo;수능완성 2회 22번&rsquo; 으로 한눈에 읽힌다.
1번부터 9번까지는 만들지 않기로 했으므로 번호는 `10` 부터 시작한다.

### lecture — 추천 강좌 (선택)

문항 카드의 **&lsquo;추천 강좌&rsquo;** 칸에 **모두에게 보이는 고정 링크**를 넣는다.
비워 두면 그 줄은 나오지 않는다.

```yaml
lecture: 최대근이 불연속인 조건 | https://youtu.be/xxxxxxxx
lecture: https://youtu.be/xxxxxxxx      # 제목 없이 주소만도 된다
```

`제목 | 주소` 에서 **마지막 `|`** 를 기준으로 가른다. 제목을 빼면 도메인이 이름이 된다.
`http`/`https` 가 아닌 주소는 화면에서 무시된다.

나은이나 아버님이 앱에서 직접 넣은 링크는 **그 기기의 localStorage** 에만 남는다.
둘 다 보게 하려면 카드의 **&lsquo;이 강좌를 모두에게 보이도록 요청하기&rsquo;** 버튼이 만들어 주는
`[ipsi_math 강좌]` 블록을 받아서 이 `lecture` 에 적고 다시 배포한다.

### parent — 어느 문항 아래에 붙는가

유사문항·사다리는 `parent` 에 **부모 문항의 ID** 를 적는다.

```yaml
id: DIF-W01-S1
parent: DIF-W01
origin: 사다리        # 또는 유사문항
source: 사다리 1단 (DIF-W01 풀이 ①)
```

`parent` 가 있는 문항은 **목록과 집계에서 빠지고 부모 카드 안에서만 보인다.**
&lsquo;기출 63문항&rsquo; 같은 숫자가 흔들리지 않게 하려는 것이다.
부모 카드 안에서 접었다 펴며 풀 수 있고, 자기만의 `알겠음 / 모름` 을 갖는다.

`origin: 사다리` 면 카드에 <b>사다리</b>, 아니면 <b>유사문항</b> 딱지가 붙는다.
부모를 못 찾으면 빌드할 때 경고가 뜬다.

딸림 문항이 하나라도 있는 부모 문항에는 카드에 **`추가생성 N`** 배지(보라)가 붙고,
단원 필터 띠에 **`추가생성 N` 칩**이 생긴다. 그 칩을 누르면 **딸림 문항이 붙은 원본만** 남는다.
단원 칩과 **겹쳐서** 쓸 수 있다 (미분 ∩ 추가생성).
칩은 점선 테두리라 단원 칩과 구분된다. 문항이 하나도 없으면 칩 자체가 안 나온다.

### origin — 어디서 온 문항인가

웹앱에서 **색상 배지와 필터 칩**의 근거가 된다. 기출이 아닌 것만 배지가 붙는다.

| 값 | 뜻 | 웹앱 표시 |
|---|---|---|
| `기출` | 평가원·교육청 기출 | 배지 없음 (기본) |
| `유사문항` | 내가 개발한 변형 문항 | 보라색 배지 + 필터 칩 |
| `오답노트` | 나은이가 모르겠다고 가져온 문제 | 문항 제목 옆 **주황색 배지** |

**새 문항을 만들 때 반드시 넣는다.** 빠뜨리면 `source`에 "자체 개발"이 있는지로 추론하지만,
`오답노트`는 추론이 불가능하므로 명시해야 한다.
`status`(진행 상태)와 혼동하지 말 것 — `origin`은 출처이고 바뀌지 않는다.

## 2-0. `[ipsi_math 강좌]` 블록이 들어오면

문항 ID와 링크가 들어 있다. 해당 문항 파일의 `lecture` 에 넣고
`python build/make_site.py` 후 커밋·푸시한다. 그래야 두 사람 화면에 모두 보인다.
제목이 없으면 링크를 열어 확인한 뒤 **한 줄짜리 제목을 붙여 준다.**

## 2-1. `[ipsi_math 질문]` 블록이 들어오면

사이트의 **&lsquo;이 내용으로 문항 만들어 달라고 하기&rsquo;** 버튼이 만들어 주는 형식이다.
블록 끝에 `parent: <문항ID>` 안내가 붙어 있다. **새로 만드는 문항에 그대로 넣는다.**
그래야 나은이 화면에서 그 문항 아래 &lsquo;이어서 풀 문항&rsquo; 자리에 들어간다.
버튼을 누른 시점에 카드에는 &lsquo;문항 요청함&rsquo; 표시가 남아 있으므로,
문항을 넣고 배포하면 그 자리가 실제 문항으로 채워진다.
문항 ID · 단원 · 출처 · 나은이가 누른 표시 · 문제 본문 · **나은이가 직접 쓴 메모**가 들어 있다.

- **메모를 그대로 진단으로 받아들인다.** "g까지는 알겠는데 그 이후가 막힌다"처럼
  경계가 적혀 있으면 사다리의 시작점을 그 경계에 맞춘다.
- **&lsquo;다른 풀이를 달라&rsquo;는 메모**는 접근 자체가 다른 길을 찾아 `## 다른 풀이`에 넣고,
  그 길만 쓰는 훈련 문항(`-A1`, `-A2`, …)을 함께 만든다. 계산 순서만 바꾼 것은 다른 풀이가 아니다.
- **&lsquo;왜 그런가&rsquo;를 묻는 메모는 못 푼 것과 다르다.** 난이도를 낮춘 문항을 주지 말고,
  그 주장이 서는 **근거를 단계로 쪼갠 사다리**(`-T1`, `-T2`, …)를 만든다.
  마지막 단에는 원래 물어본 그 문항을 다시 넣어 닫는다.
- 메모에 **해설 난이도 요구**(예: "중학생도 이해할 수 있게")가 있으면 그대로 따른다.
  용어를 줄이고 구체적인 수로 먼저 보여준 뒤 일반화한다.
- 이미 저장소에 있는 문항이면 새로 만들지 말고 그 문항의 `status`를 고치고
  `## 함정`이나 새 사다리를 덧붙인다. 없으면 3절의 표준 절차대로 등록한다.
- 처리한 뒤에는 반드시 **웹앱을 다시 만들고 재게시**한다. 안 그러면 나은이 화면에 안 보인다.

## 3. 학습자가 모르는 문제를 가져왔을 때 — 표준 절차

1. **인테이크.** 원본(사진/텍스트)을 `intake/`에 날짜 이름으로 저장한다.
2. **직접 푼다.** 답을 먼저 확정한다. 계산이 무거우면 **반드시 파이썬으로 교차검증**한다.
3. **분류.** 단원 + `topic` + `difficulty`를 정하고 문항 파일을 만든다.
   `status: wrong` (틀린 문제로 들어온 것), `source`에 출처를 적는다.
4. **핵심 요소 추출.** `core`에 **한 문장**으로 적는다. 이것이 이 저장소의 핵심 자산이다.
   "무엇을 먼저 보았어야 하는가"를 쓴다. 풀이 요약이 아니다.
5. **같은 요소를 쓰는 기존 문항을 찾는다.** `grep -r "핵심어" units/` 로 `core`와 `tags`를 훑어
   같은 발상을 요구하는 문항을 모은다. 있으면 그 문항들을 함께 세트에 넣는다.
6. **유사문항 개발.** 같은 핵심 요소를 요구하되 표면이 다른 문항을 **3~5개** 만든다.
   - 난이도는 원문항과 **동일 수준**으로 맞춘다. 쉽게 만들지 않는다.
   - **다만 학습자가 "아예 못 풀었다"고 하면 동일 수준 유사문항만으로는 또 막힌다.**
     그때는 먼저 **도입 사다리**를 만든다: 핵심 도구를 한 조각씩 심는 2~4개의 낮은 단계
     (`-S1`, `-S2`, … 로 번호를 붙이고 `source`에 "사다리 N단"을 적는다).
     마지막 단은 원문항 바로 아래 난이도로 두고, 세트의 끝에 **원문항 자체**를 넣어 닫는다.
     사다리 문항은 가능하면 **답을 손으로 전부 나열할 수 있는 크기**로 만든다 —
     규칙이 맞는지 눈으로 확인하는 것이 사다리의 목적이다.
   - 틀렸거나 헷갈린 문제라면 사다리 없이 동일 수준 유사문항으로 바로 간다.
   - 변형 축: 숫자 → 문자, 함수 종류 교체, 조건의 방향 뒤집기, 구하는 대상 바꾸기, 그래프 ↔ 식.
   - **만든 문항은 예외 없이 파이썬으로 답을 검증한다.** 검증 코드는 문항 파일 맨 아래
     `<!-- verify: ... -->` 주석으로 남기거나 `build/verify/<ID>.py`에 저장한다.
   - 답이 지저분하게 나오면(무리수, 큰 분수) 계수를 조정해 **깔끔한 답**이 되게 다시 설계한다.
7. **세트 구성.** `sets/YYYY-MM-DD_주제.txt` 에 제목과 문항 ID를 적는다.
8. **PDF 생성.** `python build/make_pdf.py --set sets/<파일>` → `dist/`에 문제지·해설지 2종.
9. **기록.** `log/progress.md`에 날짜, 들어온 문제, 진단한 약점, 처방한 세트를 한 줄로 남긴다.
10. **커밋.** `git add -A && git commit` 후 `git push`.

## 4. status 값

| 값 | 뜻 |
|---|---|
| `seed` | 기출 기반 기본 자산 |
| `wrong` | 나은이가 틀렸거나 모르겠다고 가져온 문제 |
| `variant` | 위 문제에서 파생시켜 개발한 유사문항 |
| `review` | 한 번 맞혔지만 다시 봐야 하는 문제 |
| `mastered` | 백지 재현까지 성공한 문제 |

복습 세트는 `--status wrong` 또는 `--status review`로 뽑는다.

## 5. PDF 생성 명령

```bash
python build/make_pdf.py --set sets/2026-09-10_미분.txt          # 세트로
python build/make_pdf.py --unit 05-미분 --status wrong --name 미분_오답
python build/make_pdf.py --ids DIF-004,DIF-005 --name 미니세트
python build/make_pdf.py --all --name 전체 --space 작음
```
`--space 큼|보통|작음` 으로 문제지의 풀이 공간을 조절한다 (기본 보통).
출력은 `dist/YYYYMMDD_<이름>_문제.pdf` 와 `_해설.pdf`.

## 6. 사이트 (나은이가 폰/태블릿에서 보는 화면)

**https://knuekim91.github.io/ipsi_math/** — GitHub Pages, `main` 브랜치의 `docs/` 폴더.

```bash
python build/make_site.py      # 문항을 추가/수정할 때마다 실행
git add -A && git commit && git push
```

`build/make_site.py`가 `units/`의 모든 문항을 읽어 `docs/data.js`를 만든다.
푸시하면 Pages가 1~2분 안에 자동 배포한다.
**문항만 고치고 이 스크립트를 안 돌리면 나은이 화면에 새 문제가 안 보인다.**
한 세트로 묶어서 하라.

인쇄물은 사이트에서 만들지 않는다. 문항 ID를 `sets/` 파일에 넣고
`make_pdf.py`로 문제지·해설지를 뽑는다. 이것이 확정된 경로다.

사이트는 문항별로 `알겠음 / 모름`, 자유 메모, 추천 강좌 링크를 기록한다.
(예전의 `헷갈림`은 `모름`으로 합쳤다. 옛 기록은 열 때 자동으로 옮겨진다.)
저장 위치는 로그인 여부에 따라 갈린다 — 6-3 참고.

> **옛 방식 (2026-09-06 폐기)**
> 전에는 `build/make_webapp.py`로 `dist/webapp.html`을 만들어 claude.ai Artifact로
> 게시했다. 그 스크립트와 아티팩트는 더 이상 쓰지 않는다.
> 아티팩트는 claude.ai 프레임 안에서 돌아 `window.print()`가 막히고,
> `downloads` 기능을 켜면 '링크가 있는 누구나' 공유가 막혀서
> 나은이가 계정 없이 열 수 없다는 문제가 있었다.
> **Pages로 옮긴 지금은 두 제약 모두 해당되지 않는다.**

## 6-1. 사이트 화면 구조 (docs/)

위에서부터 네 덩어리다. 블록을 늘리거나 줄일 때 이 순서를 지킨다.

| 블록 | 요소 | 내용 |
|---|---|---|
| 단원으로 찾기 | `#subject-grid` | 과목마다 카드 하나. 수Ⅰ·수Ⅱ는 단원 3개, 확통은 2개를 **한 줄**에 |
| 찾기 (2열) | `.finder` | 왼쪽 `년도별 찾기`(`#yrow` + `#exrow`), 오른쪽 `검색`(`#q` + 번호순/단원별) |
| 단원 필터 띠 | `#toolbar` | `#filt` 칩(단원 + 추가생성). **목록을 따라다니는 sticky** |
| 목록 | `#list` | 문항 카드 |

`build/make_site.py`가 빌드할 때마다 `index.html`의 `app.css` · `data.js` · `app.js`에
`?v=<타임스탬프>`를 새로 박는다. **이걸 빼면 나은이 브라우저가 옛 `data.js`를 계속 써서
새 문항이나 고친 풀이가 안 보인다.** 실제로 한 번 겪었다.

한 줄 배치는 `.ugrid`의 `--n`(그 과목의 단원 수)이 정한다. `app.js`의 `paintUnits()`가
`style="--n:3"`처럼 넣어 주므로 **CSS를 고칠 필요가 없다.**

### CSS 토큰 — 없는 이름을 쓰지 않는다

`docs/app.css` 맨 위 `:root`에 정의된 것만 쓴다.

`--bg --bg2 --card --card2 --line --line2 --tx --tx2 --tx3 --ok --mid --no --glow --r`
그리고 별칭 `--ink`(=`--tx`) `--dim`(=`--tx3`) `--disp` `--body` `--mono`.

**정의되지 않은 토큰을 쓰면 그 선언만 조용히 무효가 된다.** 한 번
`background:var(--ink)`이 통째로 날아가 선택된 버튼이 흰 바탕에 흰 글씨가 된 적이 있다.
새 색이 필요하면 `:root`와 라이트 테마 블록 **양쪽에** 추가한다.

## 6-2. 표기법 — 고등학교 교과서 기준

**나은이가 배운 기호로만 쓴다.** 대학 표기가 섞이면 풀이를 읽다가 거기서 막힌다.

| 쓰지 않는다 | 대신 |
|---|---|
| `φ(x)`, `ψ(x)` 로 치환 | **`g(x)`, `h(x)`** — 치환 함수는 알파벳으로 |
| `a ≡ b (mod n)` | "n으로 나눈 나머지가 …" |
| `⌊x⌋` | 가우스 기호 `[x]`, 또는 "… 이하의 최대 정수"로 풀어쓴다 |
| 판별식 `Δ` | **`D`** |
| "위수", "고정점", "임계점", "매개변수" | "중근", "그 값이 그대로 해가 된다", "극값을 갖는 점", "문자로 된 상수" |
| "레벨", "패리티" 같은 외래어 | "값", "홀짝" |
| `⟹` 를 &lsquo;따라서&rsquo; 대신 | "따라서", "→" |

**써도 되는 그리스 문자**는 고교 과정에 나오는 것뿐이다 —
`α β γ`(근·각), `θ`(각), `π`, `Σ`, `σ`(표준편차).
`Δ`는 증분 `Δx`로만 쓰고 판별식에는 쓰지 않는다.

### 수식 옆에 줄표(—)를 쓰지 않는다

`—`는 수식 바로 옆에 오면 **빼기로 읽힌다.** 실제로
`g(β) = β</span> — <b>x = α` 가 화면에서 `g(β) = β − x = α` 로 보여
나은이가 오타로 신고한 적이 있다. KaTeX가 수식을 렌더링하고 나면
줄표만 사이에 남아 마이너스와 구분이 안 된다.

수식과 붙는 자리에서는 줄표 대신 **`이다. 따라서`, `곧`, `,`, `&nbsp;`** 를 쓴다.
한글과 한글 사이의 줄표는 괜찮다.

`≡ ⌊ ⌋ φ ψ` 등이 새로 들어오지 않았는지 커밋 전에 훑는다.

```bash
grep -l "φ\|ψ\|≡\|⌊\|위수\|고정점\|패리티" units/*/problems/*.md

# 수식에 붙은 줄표
grep -n "</span> —\|— <span class=.m." units/*/problems/*.md
```

## 6-3. 로그인과 동기화

`server/Code.gs` 를 Google Apps Script 로 배포하고 그 `/exec` 주소를
`docs/config.js` 의 `IPSI_API` 에 넣으면 켜진다. **비워 두면 기능이 조용히 꺼지고
사이트는 기기별 localStorage 만 쓴다.** 배포 절차는 `server/README.md`.

| 파일 | 역할 |
|---|---|
| `server/Code.gs` | Apps Script. 가입·로그인·pull·push |
| `docs/config.js` | 서버 주소 한 줄 |
| `docs/auth.js` | 로그인 창, 세션, 자동 동기화 |
| `docs/app.js` | `window.IPSI` 로 상태를 내주고 `onChange` 로 변경을 알린다 |

지켜야 할 것:

- **`Content-Type` 은 반드시 `text/plain`.** `application/json` 으로 보내면
  브라우저가 사전 요청(preflight)을 보내는데 Apps Script 가 응답하지 못해 CORS 오류가 난다.
- **기록마다 `at`(수정 시각)을 남긴다.** 병합은 문항별로 `at` 이 큰 쪽이 이긴다.
  `put(id, patch)` 를 쓰면 자동으로 붙는다. `state[id] = ...` 로 직접 쓰지 말 것.
- **`window.IPSI.all()` 은 localStorage 를 다시 읽어 합친 뒤 돌려준다.**
  탭을 두 개 열었을 때 다른 탭이 쓴 것을 덮어쓰지 않기 위해서다.
- **`ipsi_math_owner` 로 기기의 주인을 기록한다.** 다른 계정으로 로그인하면
  앞사람의 기록을 합치지 않는다. 이걸 빼면 아빠 진도가 나은이 계정으로 넘어간다.

## 7. 지켜야 할 원칙

- **답을 추측해서 쓰지 않는다.** 검증하지 않은 답은 문항 파일에 넣지 않는다.
- **식이 한 줄에서 다음 줄로 넘어갈 때 무엇을 했는지 반드시 쓴다.**
  특히 **이항**과 **공통인수로 묶기**는 나은이가 가장 자주 막히는 자리다.
  `f(n) = f(n)f(n+1) − f(n−1)f(n)` 에서 곧바로
  `f(n)[f(n+1) − f(n−1) − 1] = 0` 으로 건너뛰었더니
  "이 −1이 왜 나왔지?" 라는 질문이 돌아왔다.
  넘긴 항이 묶을 때 상수로 남는 경우에는 **그 상수가 어디서 온 것인지 한 문장**을 붙인다.
  수열의 합은 `S(n)`이 아니라 **`Sₙ`** 으로 쓴다.
- **`core`는 반드시 한 문장.** 길어지면 문항을 잘못 분류한 것이다.
- **풀이는 짧게 쓰지 않는다.** 완전한 문장으로, 주어와 목적어를 넣어 쓴다.
  각 단계는 `<span class="step">① 무엇을 한다.</span>` 로 열고
  **왜 그 단계를 하는지를 먼저 쓴 뒤 식을 놓는다.**
  분량 기준: 2·3점 400자 이상, 4점 800자 이상.
  식만 나열한 풀이는 나은이가 읽다가 막힌다 — 실제로 그렇게 신고가 들어왔다.
  마지막은 항상 `답 ③` 처럼 한 줄로 닫는다.
- **유사문항은 난이도를 낮추지 않는다.** 쉬운 변형은 학습 효과가 없다.
- **PDF는 흑백 인쇄 기준.** 색으로만 구분되는 정보를 넣지 않는다.
- 단원 폴더 이름과 ID 접두사는 **바꾸지 않는다.** 과거 세트 파일이 깨진다.
